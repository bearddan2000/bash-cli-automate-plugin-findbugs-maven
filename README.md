# bash-cli-automate-plugin-findbugs-maven

## Description
Automates adding `findbugs` maven plugin.
Uses python `xml.etree` lib to add the plugin
and reformat the xml.

## Tech stack
- bash
- python

## To run
`./install.sh`
